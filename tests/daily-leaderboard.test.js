const assert = require('node:assert/strict');
const test = require('node:test');
const leaderboard = require('../daily-leaderboard');
test('display names are trimmed and constrained to 2–24 characters', () => { assert.deepEqual(leaderboard.validateDisplayName('  Dono  '), { valid: true, name: 'Dono', error: '' }); assert.equal(leaderboard.validateDisplayName('x').valid, false); assert.equal(leaderboard.validateDisplayName('x'.repeat(25)).valid, false); });
test('submission state key is isolated by date', () => assert.equal(leaderboard.submissionKey('2026-07-15'), 'dono_daily_leaderboard_submission_2026-07-15'));
test('Postgres unique constraint errors are recognised', () => { assert.equal(leaderboard.isUniqueConstraint({ code: '23505' }), true); assert.equal(leaderboard.isUniqueConstraint({ message: 'duplicate key violates unique constraint' }), true); assert.equal(leaderboard.isUniqueConstraint({ code: '42501' }), false); });
test('leaderboard sort order matches score, streak, then earliest submission', () => { const rows = [{ score: 10, best_streak: 3, created_at: '2026-07-15T12:00:00Z' }, { score: 12, best_streak: 1, created_at: '2026-07-15T13:00:00Z' }, { score: 10, best_streak: 4, created_at: '2026-07-15T14:00:00Z' }, { score: 10, best_streak: 3, created_at: '2026-07-15T11:00:00Z' }].sort(leaderboard.compareScores); assert.deepEqual(rows.map(row => row.created_at), ['2026-07-15T13:00:00Z', '2026-07-15T14:00:00Z', '2026-07-15T11:00:00Z', '2026-07-15T12:00:00Z']); });
test('only HTTPS Supabase URLs with a publishable key are accepted', () => { assert.equal(leaderboard.validConfig({ url: 'https://example.supabase.co', publishableKey: 'key' }), true); assert.equal(leaderboard.validConfig({ url: 'http://example.supabase.co', publishableKey: 'key' }), false); assert.equal(leaderboard.validConfig({ url: 'https://example.supabase.co', publishableKey: '' }), false); });
